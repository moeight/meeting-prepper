{
  "builds": [
    { "src": "api/brief.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/brief.py" },
    { "src": "/(.*)",     "dest": "public/$1"    }
  ],
  "functions": {
    "api/brief.py": {
      "maxDuration": 60
    }
  }
}
